{
  "routes": [
    {
      "method": "GET",
      "path": "/cover-image",
      "handler": "cover-image.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/cover-image",
      "handler": "cover-image.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/cover-image",
      "handler": "cover-image.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
